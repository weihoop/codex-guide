# Codex 最佳实践

## 核心原则

- 先缩小任务，再让 Codex 执行。
- 长期规范写进 `AGENTS.md`，一次性目标写进 prompt。
- 默认使用最小权限，只有必要时扩大 sandbox 或 approval。
- 让 Codex 给出验证证据，不接受“应该可以”。
- 不把个人 `~/.codex` 状态提交到仓库。

## Prompt 写法

### 好的任务说明

```text
请修复登录页移动端布局问题。
约束：只修改 src/pages/Login.tsx 和相关 CSS；保持现有设计系统；修改后运行 npm test 和 npm run lint。
输出：变更摘要、测试结果、仍需人工确认的风险。
```

### 不好的任务说明

```text
帮我优化一下项目。
```

问题是范围太大、验收标准不清、容易引入无关改动。

## 上下文管理

- 让 Codex 先搜索和读关键文件，不要一次性粘贴大量代码。
- 大任务分为“探索 → 计划 → 实现 → 验证 → 总结”。
- 每完成一个阶段，让 Codex 输出当前状态和下一步。
- 会话过长时使用 `resume` 继续同一任务，或用总结开启新会话。

## `AGENTS.md` 管理

`AGENTS.md` 应该稳定、短、可执行。建议包含：

- 项目介绍和技术栈。
- 常用命令。
- 代码风格。
- 测试和验证要求。
- 安全边界。
- 不要触碰的文件或目录。

不要写入：

- 临时需求。
- 个人偏好密钥。
- 已废弃命令。
- 和项目无关的大段教程。

## 权限最佳实践

| 场景 | 推荐 sandbox | 推荐 approval |
| --- | --- | --- |
| 阅读项目、代码审查 | `read-only` | `on-request` |
| 日常开发 | `workspace-write` | `on-request` |
| CI 文档检查 | `workspace-write` 或外部容器 | `never` |
| 生产运维 | 尽量只读 | `on-request` |
| 危险实验 | 外部隔离后再考虑 `danger-full-access` | 视情况 |

## 代码审查

当用户说“review”时，应优先找问题：

- 行为回归。
- 安全漏洞。
- 并发、边界值和错误处理。
- 测试缺口。
- 文档或迁移遗漏。

示例 prompt：

```text
请审查当前分支相对 main 的改动。只输出 findings，按严重程度排序，并给出文件/行号。没有发现问题也要说明测试覆盖缺口。
```

## 测试与验证

要求 Codex 明确运行：

- 单元测试。
- 类型检查。
- lint。
- 构建。
- 文档链接检查。
- 关键手工验收步骤。

如果某项无法运行，要说明原因和替代验证。

## 团队协作

- 团队提交 `AGENTS.md` 和脱敏配置模板。
- 每个仓库记录推荐 `codex` 启动参数。
- 审查 PR 时要求列出 Codex 参与的任务和验证命令。
- 对生产系统、账单、客户数据使用只读 token 和最小 MCP 权限。

## 避免 AI slop

- 不接受泛泛而谈的文档和注释。
- 要求 Codex 引用具体文件、具体命令、具体行为。
- 前端任务必须遵循现有设计系统；如果没有设计系统，要先确定视觉方向。
- 重构任务必须保持行为不变，并用测试或 diff 证明。
